# Use an official Python runtime as a parent image
FROM mcr.microsoft.com/vscode/devcontainers/python:3.9

# [Optional] Install additional OS packages
# Uncomment and modify the following lines if you need extra packages
# RUN sudo apt-get update && sudo apt-get install -y \
#     package1 \
#     package2 \
#     && sudo apt-get clean -y && rm -rf /var/lib/apt/lists/*

# [Optional] Set environment variables
ENV PYTHONUNBUFFERED=1

# [Optional] Copy any startup scripts
# COPY startup.sh /usr/local/bin/startup.sh
# RUN chmod +x /usr/local/bin/startup.sh

# [Optional] Run a startup script
# CMD ["bash", "/usr/local/bin/startup.sh"]
